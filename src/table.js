const main = "findie_database";
const company =  `${main}.company`
const position = `${main}.position`
const position_type = `${main}.position_type`
const submission = `${main}.submission`
const user = `${main}.user`
const application_progress = `${main}.application_progress`
const progress_type = `${main}.progress_type`
module.exports = {
    company,
    position,
    position_type,
    submission,
    user,
    application_progress,
    progress_type,
}